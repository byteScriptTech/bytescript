import { CardTitle } from '@/components/ui/card';

export const CarryOnCardTitle = () => {
  return (
    <CardTitle className="text-xl font-semibold text-gray-800">
      Carry on where you left?
    </CardTitle>
  );
};
